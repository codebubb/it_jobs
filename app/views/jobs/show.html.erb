<p id="notice"><%= notice %></p>

<h2><%= @job.title %></h2>
<h3><%= @job.company %> | £<%= @job.salary %></h3>
<p><%= @job.description.html_safe %></p>

<ul class="inline">
  <% @job.skills.each do |skill| %>
  <li><%= skill.name %></li>
  <% end %>
</ul>

<%= link_to "Apply", @job.url, class: "btn btn-block btn-default" %>

<div class="links">
  <%= link_to 'Edit', edit_job_path(@job), 'data-no-turbolink' => true %> |
  <%= link_to 'Back', root_path %>
</div>
