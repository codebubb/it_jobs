<p id="notice"><%= notice %></p>

<h1>Listing Jobs</h1>
  <% @jobs.each do |job| %>
  <div class="job">
    <%= link_to "#{job.title} @ #{job.company}", job %>
  </div>
  <% end %>


<br>

<%= link_to 'New Job', new_job_path %>
